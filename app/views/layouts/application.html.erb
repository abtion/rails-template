<!DOCTYPE html>
<html lang='en-US' class="h-full">
  <head>
    <title>Project Name Human</title>
    <meta charset='UTF-8'>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= vite_client_tag %>
    <%= vite_typescript_tag 'application' %>
    <%= vite_stylesheet_tag 'application.scss' %>
  </head>

  <body class="<%= "#{controller.controller_name} #{@body_class}".strip %> h-full">
    <main class="w-full h-full">
      <%= render "shared/nav" if controller_name != "sessions" %>
      <%= render "shared/flash_messages", container_class: "container mx-auto my-4" %>
      <%= yield %>
    </main>

  </body>
</html>
