---
en:
  helpers:
    placeholder:
      user:
        email: name@address.com
        password: Enter password
    select:
      prompt: Please select
    submit:
      create: Create %{model}
      submit: Save %{model}
      update: Update %{model}
